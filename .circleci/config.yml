---
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2026.06
    steps:
      - checkout
      - run:
          name: Build
          command: echo "Build complete"

workflows:
  ci:
    jobs:
      - build
